{
  "name": "@igrp/framework-process-studio-types",
  "version": "0.0.1-beta.1",
  "type": "module",
  "description": "IGRP Process Studio Frontend | Types",
  "keywords": [
    "igrp",
    "process",
    "studio",
    "frontend",
    "types"
  ],
  "author": "IGRP Team",
  "license": "MIT",
  "main": "./dist/index.js",
  "module": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "dev": "tsc --watch",
    "build": "rimraf dist && rimraf tsconfig.tsbuildinfo && pnpm format && tsc -b",
    "clean": "rimraf dist && rimraf tsconfig.tsbuildinfo",
    "format": "prettier --write .",
    "release": "pnpm build && yarn publish --registry=https://sonatype.nosi.cv/repository/igrp/ --tag latest"
  },
  "devDependencies": {
    "@types/node": "^24.1.0",
    "next-auth": "^4.24.0",
    "prettier": "^3.6.2",
    "rimraf": "^6.0.1",
    "typescript": "^5.8.3"
  },
  "engines": {
    "node": ">=20.x.x"
  }
}
